// Minimal layout for /planner/* routes
// Shares the same global CSS and fonts as the admin layout

export default function PlannerLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>
}
